{
  "name": "present-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter \"*\" run dev",
    "convex": "pnpm --filter @present/backend run convex"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "electron-winstaller",
      "esbuild"
    ]
  },
  "dependencies": {
    "zustand": "^5.0.12"
  }
}
