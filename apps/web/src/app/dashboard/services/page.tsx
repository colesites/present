import { DashboardClient } from "../DashboardClient";

export default function ServicesPage() {
  return (
    <DashboardClient
      org={null}
      songs={[]}
      shouldAutoOpen={false}
      section="services"
    />
  );
}
